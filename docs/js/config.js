// Claude API Configuration
// API key is stored in localStorage per-user, not in this file

const CLAUDE_CONFIG = {
    apiKey: '', // Set via localStorage - users enter their own key
    model: 'claude-sonnet-4-20250514',
    maxTokens: 1024
};
